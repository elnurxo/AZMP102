export enum MovieGenre {
  action = "action",
  drama = "drama",
  comedy = "comedy",
  horror = "horror",
  sciFi = "sciFi",
}

export enum MovieSort {
  oldest = "from-oldest",
  newest = "from-newest",
}
