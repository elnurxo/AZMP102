export class Product {
  public name: string;
  public price: number;
  public isDiscounted: boolean;
  constructor(name: string, price: number, isDiscounted: boolean) {
    this.name = name;
    this.price = price;
    this.isDiscounted = isDiscounted;
  }

  //methods
}
